---
phase: 03-accessibility-bridge-and-clipboard
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements: [COMPAT-01, COMPAT-02]

must_haves:
  truths:
    - "Text capture works in native Cocoa apps (TextEdit, Notes)"
    - "Text capture works in browser text areas (Safari or Chrome)"
    - "Clipboard is restored to original content after the capture cycle"
  artifacts: []
  key_links: []
---

<objective>
Manual verification of the accessibility bridge in real applications with real Accessibility permissions.

Purpose: COMPAT-01 and COMPAT-02 cannot be automated — they require a human to grant Accessibility permission, open real apps, type text, trigger the hotkey, and verify the captured text matches. This checkpoint confirms the bridge works end-to-end before moving to Phase 4.

Output: Human-verified confirmation that capture works in native and browser apps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-accessibility-bridge-and-clipboard/03-CONTEXT.md
@.planning/phases/03-accessibility-bridge-and-clipboard/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify text capture in native Cocoa apps (COMPAT-01)</name>
  <files>Sources/AnyVim/AccessibilityBridge.swift</files>
  <read_first>
    - .planning/phases/03-accessibility-bridge-and-clipboard/03-02-SUMMARY.md (what was built)
  </read_first>
  <action>
Build AnyVim and present the following manual test instructions to the user. No code changes — verification only.

**Prerequisites:**
- Build and run AnyVim: `xcodebuild build -project /Users/nick/Projects/any-vim/AnyVim.xcodeproj -scheme AnyVim -destination 'platform=macOS'` then launch from build products, OR run from Xcode
- Grant Accessibility permission in System Settings > Privacy and Security > Accessibility
- Grant Input Monitoring permission in System Settings > Privacy and Security > Input Monitoring
- Watch AnyVim's stdout (run from Terminal or check Xcode console)

**Test 1 — TextEdit:**
1. Open TextEdit, type "Hello from TextEdit"
2. Click into the text area (make sure cursor is there)
3. Double-tap Control key
4. Check AnyVim stdout for: `[AnyVim] Text captured to: /path/to/anyvim-*.txt`
5. Check stdout for: `[AnyVim] Original app: TextEdit`

**Test 2 — Notes:**
1. Open Notes, create a new note, type "Hello from Notes"
2. Double-tap Control key
3. Verify stdout shows capture log with "Notes" as original app

**Test 3 — Clipboard preservation:**
1. Copy something specific to clipboard (e.g., "clipboard test")
2. Open TextEdit with text, double-tap Control
3. After capture completes, Cmd+V in any app
4. Verify pasted content is "clipboard test" (original clipboard restored)
  </action>
  <verify>Human confirms all three tests pass by typing "approved"</verify>
  <acceptance_criteria>
    - AnyVim stdout shows "[AnyVim] Text captured to:" with a valid path for TextEdit test
    - AnyVim stdout shows "[AnyVim] Original app: TextEdit" for TextEdit test
    - AnyVim stdout shows capture log for Notes test
    - Clipboard content after full cycle matches pre-trigger clipboard content
  </acceptance_criteria>
  <done>Native Cocoa app text capture verified in TextEdit and Notes. Clipboard preservation confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify text capture in browser text areas (COMPAT-02)</name>
  <files>Sources/AnyVim/AccessibilityBridge.swift</files>
  <read_first>
    - .planning/phases/03-accessibility-bridge-and-clipboard/03-02-SUMMARY.md (what was built)
  </read_first>
  <action>
Present the following manual test instructions to the user. No code changes — verification only.

**Test 1 — Safari:**
1. Open Safari, navigate to any page with a text area (e.g., google.com search box, or any form)
2. Type "Hello from Safari"
3. Double-tap Control key
4. Check AnyVim stdout for capture log with "Safari" as original app

**Test 2 — Chrome (if installed):**
1. Open Chrome, navigate to a text area
2. Type "Hello from Chrome"
3. Double-tap Control key
4. Check AnyVim stdout for capture log with "Google Chrome" as original app

**Test 3 — Multi-line text area:**
1. In any browser, find a multi-line textarea (e.g., GitHub issue body, or https://jsfiddle.net)
2. Type multiple lines of text
3. Double-tap Control
4. Verify capture succeeds (stdout log appears)

**Test 4 — Empty field:**
1. Click into an empty text field in any browser
2. Double-tap Control
3. Verify AnyVim doesn't crash and stdout shows capture (temp file with empty content)
  </action>
  <verify>Human confirms all browser tests pass by typing "approved"</verify>
  <acceptance_criteria>
    - AnyVim stdout shows capture log for at least one browser (Safari)
    - Multi-line text area capture succeeds
    - Empty field capture does not crash AnyVim
    - No "Text Capture Failed" alert appears (indicating permissions are working)
  </acceptance_criteria>
  <done>Browser text area capture verified in Safari (and optionally Chrome). Empty field handling confirmed. No crashes.</done>
</task>

</tasks>

<verification>
Human confirms:
1. Text capture works in at least 2 native Cocoa apps (TextEdit, Notes)
2. Text capture works in at least 1 browser (Safari)
3. Empty field capture does not crash
4. Clipboard is preserved after the cycle
</verification>

<success_criteria>
- COMPAT-01: Native Cocoa app capture verified by human
- COMPAT-02: Browser text area capture verified by human
- No crashes or hangs during any test
- Clipboard restored after every capture cycle
</success_criteria>

<output>
After completion, create `.planning/phases/03-accessibility-bridge-and-clipboard/03-03-SUMMARY.md`
</output>
