---
phase: 02-global-hotkey-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/AnyVim/HotkeyManager.swift
  - AnyVimTests/HotkeyManagerTests.swift
autonomous: true
requirements:
  - HOTKEY-01
  - HOTKEY-02
must_haves:
  truths:
    - "HotkeyManager installs CGEventTap only when both Accessibility and Input Monitoring permissions are granted"
    - "Two Control tap-release cycles within 350ms fires the onTrigger closure"
    - "Single tap, held Control, Ctrl+key combos, and slow double-taps do NOT fire onTrigger"
    - "Tap health monitor detects disabled taps and attempts re-enable or reinstall"
  artifacts:
    - path: "Sources/AnyVim/HotkeyManager.swift"
      provides: "HotkeyManaging protocol and HotkeyManager implementation"
      exports: ["HotkeyManaging", "HotkeyManager"]
      min_lines: 120
    - path: "AnyVimTests/HotkeyManagerTests.swift"
      provides: "Unit tests for double-tap state machine and tap installation logic"
      min_lines: 80
  key_links:
    - from: "Sources/AnyVim/HotkeyManager.swift"
      to: "Sources/AnyVim/PermissionManager.swift"
      via: "install(permissionManager:) checks PermissionChecking protocol"
      pattern: "permissionManager\\.isAccessibilityGranted"
    - from: "AnyVimTests/HotkeyManagerTests.swift"
      to: "AnyVimTests/PermissionManagerTests.swift"
      via: "Reuses MockPermissionChecker from existing test infrastructure"
      pattern: "MockPermissionChecker"
---

<objective>
Implement HotkeyManager with CGEventTap-based double-tap Control detection and tap health monitoring.

Purpose: This is the core detection engine for Phase 2. The state machine and tap lifecycle must be correct and well-tested before integration with the app shell.
Output: HotkeyManaging protocol, HotkeyManager class, comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-global-hotkey-detection/02-CONTEXT.md
@.planning/phases/02-global-hotkey-detection/02-RESEARCH.md
@.planning/phases/02-global-hotkey-detection/02-VALIDATION.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From Sources/AnyVim/PermissionManager.swift:
```swift
protocol PermissionChecking {
    var isAccessibilityGranted: Bool { get }
    var isInputMonitoringGranted: Bool { get }
    func openAccessibilitySettings()
    func openInputMonitoringSettings()
}

final class PermissionManager: PermissionChecking { ... }
```

From AnyVimTests/PermissionManagerTests.swift:
```swift
final class MockPermissionChecker: PermissionChecking {
    var isAccessibilityGranted: Bool
    var isInputMonitoringGranted: Bool
    var openAccessibilitySettingsCalled = false
    var openInputMonitoringSettingsCalled = false

    init(accessibility: Bool = false, inputMonitoring: Bool = false) { ... }
    func openAccessibilitySettings() { ... }
    func openInputMonitoringSettings() { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HotkeyManaging protocol and HotkeyManager with double-tap state machine, tap lifecycle, and health monitor</name>
  <files>Sources/AnyVim/HotkeyManager.swift, AnyVimTests/HotkeyManagerTests.swift</files>
  <read_first>
    - Sources/AnyVim/PermissionManager.swift (PermissionChecking protocol — HotkeyManager.install() takes this)
    - Sources/AnyVim/AppDelegate.swift (manager ownership pattern, instance property retention)
    - AnyVimTests/PermissionManagerTests.swift (MockPermissionChecker — reuse in HotkeyManager tests)
    - AnyVimTests/MenuBarControllerTests.swift (test patterns established in Phase 1)
  </read_first>
  <behavior>
    ## State Machine Tests (testable without live CGEventTap)
    - Test: handleFlagsChanged with two Control down+up cycles within 350ms -> onTrigger fires exactly once
    - Test: handleFlagsChanged with two Control down+up cycles separated by >350ms -> onTrigger does NOT fire
    - Test: Single Control down+up -> onTrigger does NOT fire
    - Test: Control held >350ms then released -> onTrigger does NOT fire (state resets to idle)
    - Test: Control down, then Shift down (non-Control keycode), then Control down -> state resets, onTrigger does NOT fire
    - Test: Left Control first tap + Right Control second tap -> onTrigger fires (D-02: both keys count)

    ## Tap Installation Tests (protocol-mocked)
    - Test: install() with both permissions granted -> isTapInstalled returns true (using protocol mock for tap creation)
    - Test: install() with Accessibility missing -> tap NOT installed
    - Test: install() with Input Monitoring missing -> tap NOT installed
    - Test: tearDown() cleans up tap and health timer
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    Create `AnyVimTests/HotkeyManagerTests.swift`:
    - Import `XCTest` and `@testable import AnyVim`
    - Reuse `MockPermissionChecker` from `PermissionManagerTests.swift` (it is already in the test target)
    - Test the state machine by calling `handleFlagsChanged(flags:keycode:)` directly with synthetic values:
      - Control down: `flags = CGEventFlags.maskControl`, `keycode = 0x3B` (kVK_Control)
      - Control up: `flags = CGEventFlags(rawValue: 0)` (no modifiers), `keycode = 0x3B`
      - Right Control: keycode `0x3E` (kVK_RightControl)
      - Shift (intervening): `flags = CGEventFlags.maskShift`, `keycode = 0x38` (kVK_Shift)
    - For timing tests: call handleFlagsChanged for first tap, use `Thread.sleep(forTimeInterval: 0.4)` to exceed 350ms window, then call for second tap
    - Track onTrigger calls via a counter variable: `var triggerCount = 0; manager.onTrigger = { triggerCount += 1 }`
    - For tap installation tests: since CGEvent.tapCreate requires TCC permissions, test the permission-gating logic by verifying install() returns early when permissions are missing. The real tap is not testable in CI without TCC grants. Create a `TapInstalling` protocol:
      ```swift
      protocol TapInstalling {
          func createTap(callback: @escaping CGEventTapCallBack, userInfo: UnsafeMutableRawPointer?) -> CFMachPort?
          func enableTap(_ tap: CFMachPort)
          func isTapEnabled(_ tap: CFMachPort) -> Bool
          func disableTap(_ tap: CFMachPort)
      }
      ```
    - Create `MockTapInstaller` in the test file that records calls and returns a controllable result
    - HotkeyManager takes `tapInstaller: TapInstalling` parameter (defaulting to `SystemTapInstaller()` in production)

    Create `Sources/AnyVim/HotkeyManager.swift`:
    - `protocol HotkeyManaging: AnyObject` with:
      - `var isTapHealthy: Bool { get }`
      - `var onTrigger: (() -> Void)? { get set }`
      - `var onHealthChange: ((Bool) -> Void)? { get set }` (for menu bar integration in Plan 02)
      - `func install(permissionManager: PermissionChecking)`
      - `func tearDown()`
    - `@MainActor final class HotkeyManager: HotkeyManaging` with:
      - `import Carbon.HIToolbox` and `import CoreGraphics` and `import Foundation`
      - Instance properties: `eventTap: CFMachPort?`, `runLoopSource: CFRunLoopSource?`, `healthTimer: Timer?`
      - `private(set) var isTapHealthy: Bool = false` — calls `onHealthChange?(newValue)` via didSet
      - `var onTrigger: (() -> Void)?`
      - `var onHealthChange: ((Bool) -> Void)?`
      - `private let tapInstaller: TapInstalling` — injected, defaults to `SystemTapInstaller()`
      - Double-tap state machine enum: `.idle`, `.firstTapDown(at: TimeInterval)`, `.firstTapUp(at: TimeInterval)`, `.secondTapDown`
      - `private let controlKeycodes: Set<Int> = [Int(kVK_Control), Int(kVK_RightControl)]` (per D-02)
      - `private let doubleTapThreshold: TimeInterval = 0.350` (per D-03)
      - `func install(permissionManager: PermissionChecking)` — guard both `isAccessibilityGranted` and `isInputMonitoringGranted`, then call `installTap()` and `startHealthMonitor()` (per D-06)
      - `private func installTap()` — create event tap for `.flagsChanged` events only (per D-01), using `.cgSessionEventTap`, `.headInsertEventTap`, `.defaultTap` (per D-07), pass `self` as userInfo via `Unmanaged.passUnretained(self).toOpaque()`, add to `CFRunLoopGetMain()` with `.commonModes`
      - `private func tearDownTap()` — remove RunLoop source FIRST, then nil out tap and source (per anti-pattern: tear down RunLoop source before tap)
      - `func tearDown()` — invalidate health timer, call tearDownTap()
      - `func handleFlagsChanged(flags: CGEventFlags, keycode: Int)` — the state machine logic per D-01/D-02/D-03/D-04. Check `controlKeycodes.contains(keycode)`, if not Control reset to `.idle` (D-04). Use `ProcessInfo.processInfo.systemUptime` for monotonic timing. State transitions: idle->firstTapDown (Control pressed), firstTapDown->firstTapUp (Control released within 350ms) or firstTapDown->idle (held >350ms), firstTapUp->secondTapDown (Control pressed within 350ms) or firstTapUp->idle (>350ms), secondTapDown->idle+fire (Control released)
      - `func handleTapDisabled()` — re-enable via `CGEvent.tapEnable(tap:enable:)` (per anti-pattern: handle .tapDisabledByTimeout/.tapDisabledByUserInput in callback)
      - `private func startHealthMonitor()` — `Timer.scheduledTimer(withTimeInterval: 5.0, repeats: true)` calling `checkTapHealth()` (per D-08)
      - `private func checkTapHealth()` — check `tapInstaller.isTapEnabled(tap)`, try `tapInstaller.enableTap(tap)`, if still not enabled tearDownTap()+installTap() (per D-08)
    - `struct SystemTapInstaller: TapInstalling` — wraps real CGEvent.tapCreate/tapEnable/tapIsEnabled
    - Global C callback function `hotkeyEventTapCallback(proxy:type:event:userInfo:)` — extract `Unmanaged<HotkeyManager>` from userInfo, handle `.tapDisabledByTimeout`/`.tapDisabledByUserInput` by bouncing to `DispatchQueue.main.async { manager.handleTapDisabled() }`, handle `.flagsChanged` by snapshotting `event.flags` and `event.getIntegerValueField(.keyboardEventKeycode)` then bouncing to `DispatchQueue.main.async { manager.handleFlagsChanged(flags:keycode:) }`, return `Unmanaged.passRetained(event)` for all events (do not consume per D-07 — just observe)

    **Swift 6 compliance:** The C callback is a free function (nonisolated). All `@MainActor` state mutation happens via `DispatchQueue.main.async`. The `handleFlagsChanged` and `handleTapDisabled` methods are on the `@MainActor` class and called from main queue.
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/any-vim && xcodebuild test -project AnyVim.xcodeproj -scheme AnyVim -destination 'platform=macOS' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `Sources/AnyVim/HotkeyManager.swift` exists and contains `protocol HotkeyManaging`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `@MainActor final class HotkeyManager: HotkeyManaging`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `protocol TapInstalling`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `import Carbon.HIToolbox`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `case idle` and `case firstTapDown` and `case firstTapUp` and `case secondTapDown`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `controlKeycodes` with values `kVK_Control` and `kVK_RightControl`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `0.350` (threshold)
    - File `Sources/AnyVim/HotkeyManager.swift` contains `.cgSessionEventTap` and `.headInsertEventTap` and `.defaultTap`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `CGEvent.tapIsEnabled`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `hotkeyEventTapCallback`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `DispatchQueue.main.async`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `ProcessInfo.processInfo.systemUptime`
    - File `Sources/AnyVim/HotkeyManager.swift` contains `onHealthChange`
    - File `AnyVimTests/HotkeyManagerTests.swift` exists and contains `testDoubleTapWithin350msFires`
    - File `AnyVimTests/HotkeyManagerTests.swift` contains `testDoubleTapOutside350msDoesNotFire`
    - File `AnyVimTests/HotkeyManagerTests.swift` contains `testSingleTapDoesNotFire`
    - File `AnyVimTests/HotkeyManagerTests.swift` contains `testHeldControlDoesNotFire`
    - File `AnyVimTests/HotkeyManagerTests.swift` contains `testInterveningKeyResetsStateMachine`
    - File `AnyVimTests/HotkeyManagerTests.swift` contains `MockTapInstaller`
    - `xcodebuild test` passes with zero failures
  </acceptance_criteria>
  <done>
    HotkeyManaging protocol and HotkeyManager class exist with CGEventTap lifecycle, double-tap state machine, and health monitor. All unit tests pass: double-tap within 350ms fires, slow double-tap does not fire, single tap does not fire, held Control does not fire, intervening keys reset state, both left/right Control work, tap installation is gated on permissions.
  </done>
</task>

</tasks>

<verification>
Run full test suite: `xcodebuild test -project AnyVim.xcodeproj -scheme AnyVim -destination 'platform=macOS'`
All existing Phase 1 tests still pass. All new HotkeyManagerTests pass.
</verification>

<success_criteria>
- HotkeyManaging protocol exported for use by AppDelegate and MenuBarController
- HotkeyManager double-tap state machine correctly distinguishes tap vs hold vs combo
- Tap health monitor detects and recovers from disabled taps
- All tests green including existing Phase 1 tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-global-hotkey-detection/02-01-SUMMARY.md`
</output>
