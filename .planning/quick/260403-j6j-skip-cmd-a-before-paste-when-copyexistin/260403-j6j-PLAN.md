---
phase: quick
plan: 260403-j6j
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/AnyVim/EditCycleCoordinating.swift
  - Sources/AnyVim/AccessibilityBridge.swift
  - Sources/AnyVim/AppDelegate.swift
autonomous: true
requirements: [skip-cmd-a-when-no-capture]
must_haves:
  truths:
    - "When copyExistingText is off, :wq pastes at cursor position without selecting all first"
    - "When copyExistingText is on, :wq still does Cmd+A then Cmd+V (existing behavior unchanged)"
  artifacts:
    - path: "Sources/AnyVim/EditCycleCoordinating.swift"
      provides: "Updated TextCapturing protocol with selectAllBeforePaste parameter"
    - path: "Sources/AnyVim/AccessibilityBridge.swift"
      provides: "Conditional Cmd+A in restoreText based on selectAllBeforePaste"
    - path: "Sources/AnyVim/AppDelegate.swift"
      provides: "Passes copyExistingText as selectAllBeforePaste to restoreText"
  key_links:
    - from: "Sources/AnyVim/AppDelegate.swift"
      to: "Sources/AnyVim/AccessibilityBridge.swift"
      via: "restoreText call with selectAllBeforePaste argument"
      pattern: "restoreText.*selectAllBeforePaste"
---

<objective>
Skip Cmd+A (select all) before Cmd+V (paste) when the user opened vim with an empty buffer (copyExistingText is off). When no existing text was captured, there is nothing to replace — the edited text should be inserted at the cursor position, not replace the entire field.

Purpose: Fix incorrect behavior where pasting after an empty-buffer edit replaces the entire text field contents instead of inserting at cursor.
Output: Updated restoreText method that conditionally skips Cmd+A based on whether text was originally captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Sources/AnyVim/EditCycleCoordinating.swift
@Sources/AnyVim/AccessibilityBridge.swift
@Sources/AnyVim/AppDelegate.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selectAllBeforePaste parameter to restoreText across protocol and implementation</name>
  <files>Sources/AnyVim/EditCycleCoordinating.swift, Sources/AnyVim/AccessibilityBridge.swift, Sources/AnyVim/AppDelegate.swift</files>
  <action>
1. In EditCycleCoordinating.swift, update the TextCapturing protocol's restoreText signature to add a `selectAllBeforePaste: Bool` parameter:
   `func restoreText(_ editedContent: String, captureResult: CaptureResult, selectAllBeforePaste: Bool) async`

2. In AccessibilityBridge.swift, update the restoreText method (line ~142) to accept the new parameter. Wrap the Cmd+A keystroke (line ~157) in an `if selectAllBeforePaste { ... }` block. The Cmd+V keystroke and everything else stays unconditional. The sleep between Cmd+A and Cmd+V should also be inside the `if` block since it is only needed to let the selection complete before pasting.

3. In AppDelegate.swift, update the restoreText call (line ~177) to pass `selectAllBeforePaste: copyExistingText` where `copyExistingText` is read from `UserDefaults.standard.bool(forKey: "copyExistingText")`. The value is already read on line ~157 — either capture it in a local `let` before the `if` block or re-read it at the call site.
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/any-vim && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>restoreText accepts selectAllBeforePaste parameter. When false, Cmd+A is skipped and only Cmd+V is sent. When true, existing Cmd+A then Cmd+V behavior is preserved. Project compiles cleanly.</done>
</task>

</tasks>

<verification>
`swift build` compiles without errors or warnings related to the changed files.
</verification>

<success_criteria>
- Protocol, implementation, and call site all use the new selectAllBeforePaste parameter
- copyExistingText=true path: Cmd+A -> sleep -> Cmd+V (unchanged behavior)
- copyExistingText=false path: Cmd+V only (no Cmd+A, no unnecessary sleep)
- Clean compilation
</success_criteria>

<output>
After completion, create `.planning/quick/260403-j6j-skip-cmd-a-before-paste-when-copyexistin/260403-j6j-SUMMARY.md`
</output>
